[package]
name = "ncm_inject"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
deno_runtime = { version = "0.135.0" }
tokio = { version = "1", features = ["full"] }

# Compile to a dll lib
[lib]
crate-type = ["cdylib"]
