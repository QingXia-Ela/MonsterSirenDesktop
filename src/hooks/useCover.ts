const coverEle = document.createElement("div");
coverEle.id = "cover";
coverEle.setAttribute("style", "position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0, 0, 0, 0.5); z-index: 10;");

function UseCover() {

  return;
}

export default UseCover;