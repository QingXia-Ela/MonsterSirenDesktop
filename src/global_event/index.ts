/**
 * see rust code at [src-tauri/global_event.rs](src/global_event.rs)
 *
 * this should keep same file structure with `src-tauri`
 */
import './audio_instance';
import './store';
import './frontend_notify';
